services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: osticket
      MYSQL_DATABASE: osticket
    ports:
      - "3306:3306"
  web:
    build:
      context: docker/php
    volumes:
      - ./:/var/www/html
    ports:
      - "8080:80"
    depends_on:
      - db
